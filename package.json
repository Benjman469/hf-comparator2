{
  "name": "hf-comparator",
  "version": "1.0.0",
  "scripts": {
    "start": "vercel dev",
    "postinstall": "npx playwright install-deps && npx playwright install"
  },
  "dependencies": {
    "chrome-aws-lambda": "^10.1.0",
    "playwright-core": "^10.4.0",
    "node-fetch": "^3.3.1"
  }
}
