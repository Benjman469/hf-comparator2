{
  "name": "hf-comparator",
  "version": "1.0.0",
  "scripts": {
    "start": "vercel dev",
    "postinstall": "npx playwright install-deps && npx playwright install"
  },
  "dependencies": {
    "playwright-core": "^1.39.1",
    "node-fetch": "^3.3.1"
  }
}
